enum CategoryType { daily, subscription, installment, vehicle }
enum BillingCycle { weekly, monthly, yearly }
enum InstallmentFrequency { weekly, biweekly, monthly }
enum PaymentStatus { pending, paid, overdue, failed }
enum FuelType { gasoline, diesel, electric, hybrid, lpg }
enum MaintenanceCategory { tagliando, pneumatici, freni, elettrico, batteria, carrozzeria, altro }
